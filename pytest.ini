[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    # Suppress known benign RuntimeWarning emitted by AsyncMock in tests when
    # simulating first-refresh timeouts; tracked in tests via caplog.
    ignore:coroutine '.*' was never awaited:RuntimeWarning
